# 🚀 Як запустити BlackZone Forum?

## Швидкий старт (5 хвилин)

### 1. Встановіть Node.js
Завантажте з: https://nodejs.org/ (версія 18+)

### 2. Встановіть MongoDB
Вибирайте один варіант:

**A) Локально (на вашому комп'ютері):**
- Завантажте: https://www.mongodb.com/try/download/community
- Встановіть
- Запустіть MongoDB (mongod.exe на Windows)

**B) У хмарі (рекомендується):**
- Перейти на: https://www.mongodb.com/cloud/atlas
- Зареєструватись (безплатно)
- Створити новий кластер
- Отримати connection string

### 3. Встановлення залежностей

**Windows:**
```bash
cd C:\Users\dmitr\OneDrive\Desktop\BlackZone-Forum
setup.bat
```

**Mac/Linux:**
```bash
cd ~/Desktop/BlackZone-Forum
chmod +x setup.sh
./setup.sh
```

**Вручну:**
```bash
cd server
npm install
cd ..

cd client
npm install
cd ..
```

### 4. Запуск

**Terminal 1:**
```bash
cd server
npm start
```

**Terminal 2:**
```bash
cd client
npm start
```

**Результат:**
- Frontend: http://localhost:3000
- Backend: http://localhost:5000

### 5. Вхід

Використовуйте акаунт:
- Username: `Dmitro`
- Password: `09102014`

---

## Розгортання на хостинг

### 🔥 Найпростіший варіант: Railway

1. Перейти на: https://railway.app
2. Зареєструватись через GitHub
3. Натиснути "New Project"
4. "Deploy from GitHub repo"
5. Вибрати свій репозиторій
6. "Add Service" → "MongoDB"
7. Мага їм розгорнеться все автоматично!

### 🚂 Heroku (якщо Railway платна)

```bash
heroku login
heroku create your-app-name
heroku config:set MONGODB_URI="ваш mongodb url"
heroku config:set JWT_SECRET="random-string"
git push heroku main
```

---

## Проблеми та рішення

### ❌ "npm not found"
**Рішення:** Перезавантажте комп'ютер після встановлення Node.js

### ❌ "MongoDB not found"
**Рішення:** 
- Перевірте, що MongoDB запущена
- Або використовуйте MongoDB Atlas

### ❌ "Port 3000 is already in use"
**Рішення:**
```bash
# Kill процес на порту 3000
# Windows:
netstat -ano | findstr :3000
taskkill /PID <PID> /F

# Mac/Linux:
lsof -i :3000
kill -9 <PID>
```

---

## Функції

✅ Реєстрація та логін
✅ Форум з категоріями
✅ Теми та повідомлення
✅ Адміністративна панель
✅ Управління користувачами
✅ Система ролей

---

## Файли конфігу

- `server/.env` - Налаштування backend
- `client/.env` - Налаштування frontend (якщо потрібно)

---

## Контакти

Якщо виникли проблеми:
1. Перевірте консоль (F12 в браузері)
2. Прочитайте GUIDE_UA.md
3. Перевірте логи сервера

**Успіхів! 🎉**
